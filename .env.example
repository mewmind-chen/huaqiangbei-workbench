# Optional outbound enrichment. Never reuse this value for Workbench's own API.
AGENT_API_URL=http://127.0.0.1:8787
ELECTRONICS_AGENT_PLATFORM_TOKEN=

# Optional inbound protection for /api/agent/* and the local Harness/MCP bridge.
# Never reuse this value for electronics-agent-platform.
WORKBENCH_AGENT_API_TOKEN=
